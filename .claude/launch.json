{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "alpha-colorlab",
      "runtimeExecutable": "npm",
      "runtimeArgs": ["run", "dev"],
      "port": 3000,
      "autoPort": false
    },
    {
      "// ": "Seed-only preview: blanks the Supabase vars so the app takes its seed fallback, which is the only way to see freshly emitted recipes before they are pushed. Real process env wins over .env.local in Next, so this leaves that file untouched. Port 3001 keeps 3000 free for the OAuth callback.",
      "name": "alpha-colorlab-seed",
      "runtimeExecutable": "env",
      "runtimeArgs": [
        "NEXT_PUBLIC_SUPABASE_URL=",
        "NEXT_PUBLIC_SUPABASE_ANON_KEY=",
        "npm",
        "run",
        "dev",
        "--",
        "--port",
        "3001"
      ],
      "port": 3001,
      "autoPort": false
    }
  ]
}
